import {styled} from '../styles/styled'
import {media} from '../styles/media'

export const PageLayout = styled.div`
    margin: 0 auto;
    padding: 50px 46px;
    ${media.phone`
        padding: 40px 16px;
    `}
` 
